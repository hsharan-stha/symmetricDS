import { ScrollPaginationDirective } from './scroll-pagination.directive';

describe('ScrollPaginationDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollPaginationDirective();
    expect(directive).toBeTruthy();
  });
});
